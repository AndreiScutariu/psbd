CREATE TABLE MEDICAL_RESULT 
(	
	ID NUMBER(11,0) NOT NULL ENABLE, 
	APPOINTMENT_ID NUMBER(11, 0), 
	DESCRIPTION VARCHAR2(100 BYTE), 
	CREATED_DATE DATE, 
	CONSTRAINT MEDICAL_RESULT_PK PRIMARY KEY (ID),
	CONSTRAINT APPONTMENT_ID_FK FOREIGN KEY (APPOINTMENT_ID) REFERENCES APPOINTMENT (ID) ENABLE
);
